# Interruptor (app)

Menubar macOS. Soft disconnect do monitor externo.

```bash
./build.sh
open Interruptor.app
```

API privada SkyLight (`SLSConfigureDisplayEnabled`). Pode mudar em updates da Apple.
